# Dependencies for the "moduui-timer" MCP server
# Install:  pip install -r requirements.txt
# NOTE: comments are kept ASCII-only on purpose -- Korean Windows (cp949)
#       fails to parse non-ASCII requirements files with some pip versions.

# Official MCP SDK. Pinned below v2 (v2 release expected ~2026-07-28).
mcp>=1.27,<2

# HTTP client used for timer creation (POST /create)
httpx>=0.27

# socket.io client used for timer control (start/pause/reset/...)
# Server runs socket.io 4.5.4 -> compatible with 5.x clients.
python-socketio[client]>=5,<6
